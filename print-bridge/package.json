{
  "name": "zodu-print-bridge",
  "version": "1.0.0",
  "description": "Local print bridge: lets the Zodu restaurant POS send KOTs to LAN, USB and Bluetooth thermal printers.",
  "main": "src/server.js",
  "private": true,
  "scripts": {
    "start": "node src/server.js",
    "build:exe": "pkg src/server.js --targets node18-win-x64 --output dist/ZoduPrintBridge.exe"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "ISC"
}
